unusedStructMember
useStlAlgorithm

# The one implicit single-argument constructor in the tree, deliberate: a
# FilterFunctionT is meant to be constructible from a bare callable. Scoped to
# that file so the check still fires anywhere else.
noExplicitConstructor:*/genetic/generation.hpp

# tlsf::SectionEntry is implicitly constructible from a Formula on purpose, so
# that the parser and the test fixtures keep building sections out of formulae
# unchanged; the conversion the tombstone needs to police is the one back to
# Formula, which does not exist. Inline suppression cannot be used here: it must
# sit on the line above the constructor, which is where the clang-tidy
# NOLINTNEXTLINE for the same deliberate choice already sits.
noExplicitConstructor:*/tlsf/specification.hpp

# next_bool() is non-pure — each call returns a different value, so the
# "identical condition" check is a false positive here.
identicalConditionAfterEarlyExit

# benchmark::State must be non-const (its begin()/end() advance internal
# iteration state). cppcheck doesn't know this and suggests const.
constParameterCallback

# bounded_uniform draws from the engine, so its std::mt19937& cannot be const:
# operator() is non-const. cppcheck does not model that for a standard-library
# type and suggests the reference be tightened. Scoped to the one header that
# owns a generator, so the check still fires elsewhere.
constParameterReference:*/genetic/random_source.hpp

# SpawnGuard holds spawn_mutex across [pipe creation, fork] on macOS, and is an
# empty class everywhere else -- Linux has pipe2, which closes that window in
# the kernel, so serialising forks there would cost concurrency for nothing.
# cppcheck analyses with neither platform's macros set, so what it sees is a
# local whose constructor and destructor are both empty, and it is right about
# that configuration. Scoped to the one file that spawns, so the check still
# fires on a genuinely dead local anywhere else.
unusedVariable:*/runner/process.cpp
