
counter: genetic repair of unrealizable specifications

  realize $COUNTER_EXAMPLES/lily02/spec.tlsf
  mkdir -p out && counter --input $COUNTER_EXAMPLES/lily02/spec.tlsf --output-dir out --seed 42

Every binary answers --help. The user is `counter`, password `counter`,
with sudo; change the password with `passwd`. From the host, ssh and scp
reach this machine on 127.0.0.1 port 2222.

