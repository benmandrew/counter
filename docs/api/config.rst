config.hpp
==========

Runtime algorithm parameters for the genetic repair loop. All members have
default values and can be overridden at runtime via a TOML configuration file
(see :doc:`config_io`).

.. doxygenfile:: config.hpp
