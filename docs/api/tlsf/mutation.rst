tlsf/mutation.hpp
=================

The mutation operator for ``tlsf::Specification``. It rewrites exactly one
section formula's propositional subtrees while preserving its temporal
skeleton, choosing an assumption- or guarantee-side section by a fixed
probability and drawing replacement atoms from the inputs (assumption side) or
inputs and outputs (guarantee side).

.. doxygenfile:: tlsf/mutation.hpp
