{
  "assumptions": [],
  "guarantees": [
    {
      "condition": "true",
      "condition-type": "trigger",
      "response": "(state_nominal & limits) -> STATE_FAULT",
      "timing": { "type": "Always" }
    },
    {
      "condition": "true",
      "condition-type": "trigger",
      "response": "(state_nominal & !request & !limits) -> STATE_TRANSITION",
      "timing": { "type": "Always" }
    },
    {
      "condition": "true",
      "condition-type": "continual",
      "response": "!(STATE_FAULT & STATE_TRANSITION)",
      "timing": { "type": "Always" },
      "weakenable": false
    }
  ],
  "in_atoms": ["state_nominal", "limits", "request"],
  "out_atoms": ["STATE_FAULT", "STATE_TRANSITION"]
}
