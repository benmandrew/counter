{
  "campaign": "aurus-h2h",
  "attribution": "recorded",
  "generated_on": "2026-08-17",
  "status": "partial: AuRUS arm complete, counter arm not yet run",
  "method": "Recorded at run time, not reconstructed. The AuRUS arm was staged, launched, monitored and closed by the session that authored its harness; this file restates those records. The run window is taken from run.log mtimes in the collected output tree, which the harness writes as each run finishes.",
  "profile_commit": {
    "sha": "dea51b7b2f7a7179229c6ebc74c8b4c4f6bc9a55",
    "subject": "fix(experiments): run AuRUS at the pre-fork base, not this project's fork",
    "author_date": "2026-08-14 17:31",
    "commit_date": "2026-08-14 17:31",
    "reachable_from_main": false,
    "must_resolve": false,
    "note": "Informational, and deliberately not held by a tag. No counter binary ran in this arm, so this sha pins nothing that determines a result; it names the revision the harness came from, and the harness itself is pinned exactly by the content-addressed blob under vendored_scripts. The 2026-08-17 rebase onto main rewrote this sha (the equivalent post-rebase commit is 87ec8db) and left the vendored blob byte-identical, which is why the rebase costs this record nothing. The version that does determine the results is under `aurus` below, and that one is tagged."
  },
  "profile_commit_confidence": "recorded",
  "profile_commit_evidence": "Both hosts were staged to this commit on branch feat/aurus-h2h before launch, and each host's on-disk scripts/aurus_campaign.py still hashes to the blob this commit records (md5 c35d02c0adf2c5ad43fed052004e2b04, checked 2026-08-17). It is the commit that fixed the corpus at the paper's 26 rows and pointed the harness at the pre-fork AuRUS base.",
  "binary_commit": null,
  "binary_commit_note": "Null by construction rather than by loss. The AuRUS arm invokes unreal-repair.sh alone and links no counter binary, so there is nothing to stamp. The counter arm, when it runs, records its own commit through the --version stamping in run_experiments.py and this field must be filled in then.",
  "aurus": {
    "repo": "git@github.com:benmandrew/AuRUS.git (fork of the authors' unreal-repair; also pushed to git@ssh.git.benmandrew.com:me/AuRUS.git)",
    "note": "This is the version that determines every result in this file, and it lives in a different repository, so no tag in the counter repo can hold it. The three fields below are the record: `commit` is the upstream base, `deployed_commit` is the exact tree that ran, and `tracked_file_digest` verifies a deployment without needing either repository.",
    "commit": "3f6f01fad3404724d1cca33ad30ab4c63fe61bad",
    "subject": "minors",
    "author_date": "2024-05-28",
    "commit_reachable": "An ancestor of the branches base, master and spot-ltl, and present on both remotes, so it is held without a tag of its own. It is not a branch tip: master's HEAD is ccb42ea, which carries the fork optimisations this campaign excluded, so checking out master reproduces the wrong thing.",
    "pin": "The campaign is pinned at this commit plus the single documented Strix substitution below. Reproduce by checking out 3f6f01f and replacing lib/new_strix/strix with a Linux build of Strix 21.0.0.",
    "tracked_file_digest": {
      "value": "43ab120e2ea5c44aeed0d9ed49100d99d4dbe99fbcb7812088ac46f828cf9e02",
      "over": "the 6713 tracked files of 3f6f01f, with the Strix substitution applied",
      "recipe": "git ls-files | LC_ALL=C sort | xargs sha256sum | sha256sum",
      "verified": "2026-08-17: identical on the local worktree, av2 and av3. A whole-directory digest deliberately is not used, because `ant compile` writes bin/ class files that differ per host and are not part of what ran.",
      "purpose": "Checks a deployment against what ran using nothing but the files on disk, so the archive stays verifiable if the AuRUS repository becomes unavailable."
    },
    "deployment": "rsynced to both hosts at ~/projects/tools/aurus-base without .git, so the hosts carry the tree and not the history.",
    "deviations": [
      {
        "path": "lib/new_strix/strix",
        "tracked_blob": "ff6cb37cf74d4d97e96a252dafb706edee336e5f",
        "deployed_blob": "db8b56609061e28b8ebfa3f6dff9468b6abb26fe",
        "reason": "The blob 3f6f01f tracks is a macOS Mach-O executable. On Linux it does not run, realizability checking fails, and every run returns zero solutions. This is the only file that differs from the base tree, and the substitution is unavoidable on this hardware.",
        "same_version": "Both blobs are Strix 21.0.0. The deployed one answers `--version` with `strix 21.0.0`; the macOS blob carries the same version string and the same CLI strings. The swap is a platform build, so it changes no behaviour of the realizability check.",
        "blob_origin": "The deployed blob was introduced by fork commit 6546a6c (`Split semantic factor into strengthening and weakening`), which is a descendant of 3f6f01f rather than upstream, and is the blob the fork still tracks at ccb42ea. So the bytes come from the fork's history even though the version matches upstream's. Nothing else from that commit is present.",
        "verified": "file(1) reports ELF 64-bit LSB pie executable on both hosts; no run.log contains an ERR: lib/new_strix/strix line."
      }
    ],
    "not_deviations": "Deliberately absent: the fork's SPOT satisfiability swap, its solver cache, and its parallel ranking. The arm runs the authors' algorithm as they left it."
  },
  "flags": {
    "base": "-Max=1000 -Gen=1000 -Pop=100 -k=20 -addA -geneNUM=0 -factors=0.7,0.1,0.2 -GATO=7200",
    "factors_note": "Settings.setFactors halves the semantic weight into LOST_MODELS_FACTOR and WON_MODELS_FACTOR, so -factors=0.7,0.1,0.2 is the 0.7/0.1/0.1/0.1 weighting requested. It is also the shipped default and what two of the three upstream drivers use.",
    "only_inputs_a": [
      "arbiter-aurus", "minepump", "rg1", "rg2", "lift",
      "gyro-var1", "gyro-var2", "humanoid-458", "humanoid-531"
    ],
    "only_inputs_a_basis": "The nine families the upstream drivers pass -onlyInputsA for; no per-group -factors override was applied, at the user's direction.",
    "no_reference_flags": "No -ref flags. Scoring is against this project's examples/<spec>/fixes through the compare binary, so both arms are one statistic over two populations. AuRUS's own case-studies/*/genuine/ directories are never read."
  },
  "run_window": {
    "earliest": "2026-08-14T16:32:45Z",
    "latest": "2026-08-15T16:46:22Z",
    "note": "From run.log mtimes across the collected tree. av3's clock runs about 17 minutes fast, so its half carries that skew. Wall time per host as the harness reported it: av2 1422.4 min, av3 1455.5 min."
  },
  "hosts": {
    "av2": { "repeats": "0-14", "rows": 390, "output_bytes": "772M" },
    "av3": { "repeats": "15-29", "rows": 390, "output_bytes": "747M" }
  },
  "corpus": "The 26 TLSF specifications of the AuRUS paper, at the paths named in SPEC_TLSF. Six carry an -aurus suffix because this project has same-named families at different SYNTCOMP parameter instances. lily02 points at case-studies/syntcomp-unreal/lily02/lilydemo02.tlsf, the paper's row, whose file is byte-identical to the top-level copy.",
  "results": {
    "rows": 780,
    "completed": 605,
    "capped_at_gato": 173,
    "killed_by_oom": 2,
    "zero_solution_completions": 9,
    "solutions_written": 287006,
    "integrity": "The n_solutions column sums to 287006 and the collected tree holds exactly 287006 spec*.tlsf files. All 780 rows have a run.log."
  },
  "well_separation_sweep": {
    "run_on": "2026-08-17",
    "scope": "full: every one of the 287,006 candidate repairs, not a sample",
    "tool": "scripts/check_well_separated.py, which re-derives the query from the TLSF text and calls ltlsynt directly. Deliberately not counter's in-run checker, which maps a realizability timeout to unrealizable and caches it -- for this query that reads as well-separated, so a verdict taken from it would launder timeouts into passes.",
    "fast_path": "off, so every verdict came from ltlsynt. Exactly 2 ill-separated files had input-only assumptions and would have been misclassified as well-separated by the fast path.",
    "ill_separated": 113958,
    "well_separated": 173048,
    "undecided": 0,
    "rate": "39.71%",
    "cost": "1.99 core-hours; mean 0.025s per ltlsynt call, max 0.25s against a 60s timeout that was never approached.",
    "meaning": "AuRUS ran with -addA and may add assumptions freely, so a repair can reach realizability by adding assumptions the system then defeats. Two in five of its claimed repairs do exactly that. Any comparison quoting AuRUS's repair count without this correction overstates the baseline by that margin.",
    "distribution": "Bimodal rather than uniform, so the per-family split matters more than the mean: lily16 98.1% and load-balancer-aurus 91.4% at one end; rg2, arbiter-aurus and humanoid-458 at 0.0% and rg1 at one file out of 7442 at the other.",
    "families_with_no_output": "Five of the 26 emitted no repairs at all across 30 repeats each -- full-arbiter-aurus, humanoid-503, humanoid-531, humanoid-741, prioritized-arbiter-aurus -- so they contribute no denominator. They are exactly the five that capped 30/30 in the results CSV, two independent measurements agreeing.",
    "denominator_caveat": "Measured over every emitted file rather than only those counter's `realize` confirms realizable. AuRUS emits only what Strix accepts, so the two should be close, but the exact figure waits on the aurus_validate run, which produces both columns in one pass.",
    "csvs": "experiments/aurus-h2h/wellsep-av2.csv and wellsep-av3.csv, gitignored with the rest of the results. The sweep ran on each host over the half already on its disk; the two row counts sum to 287006 exactly.",
    "csv_note": "The sweep tool's arm/spec columns are empty here: parse_run_id expects run_experiments' `sweep_..._seedNN` directory layout and the AuRUS tree is <family>/repeat-NN/, so family attribution comes from the path column."
  },
  "caveats": [
    "The counter arm has not run. This directory records one side of a head-to-head, and no comparison may be drawn from it alone.",
    "AuRUS is not seedable: Settings.RANDOM_GENERATOR draws from Math.random(). The mitigation is 30 repeats per family with every run's log and solution set archived, the same mitigation the 2026-07-24 campaign used.",
    "Two full-arbiter-aurus runs (av2 repeat-04, av3 repeat-20) exited 137 with an empty n_solutions and no killed flag. The JVM was SIGKILLed under -Xmx8g while the harness timeout had not fired, which is the kernel OOM killer rather than a cap. They are failures and belong in neither the completed nor the capped bucket.",
    "Cap rate concentrates in six families. humanoid-503, humanoid-531 and humanoid-741 capped 30 of 30; full-arbiter-aurus produced nothing at all (28 capped plus the 2 OOM rows); prioritized-arbiter-aurus capped 29 of 30 and pcar-v2-888 25 of 30. lily02 capped once. The other 20 families capped zero times.",
    "Completing runs finish far below the budget: median wall 150 s against a 7200 s cap, maximum 6508 s. The capped runs are hard rather than marginal, so a longer budget would not convert many of them.",
    "The macOS Strix substitution above is a deviation from the base tree and is not optional on Linux.",
    "The counter arm will run against different engine code than the branch carried while the AuRUS arm ran. The 2026-08-17 rebase brought in ten commits, three of which change behaviour: selection_scheme now defaults to nsga2-apportion (c71ecf0), the MRS walk defaults to conflict-degree admission order (aa050f6), and crossover grafts donors from anywhere in the parent (5f2ae7b). None of this touches the AuRUS arm, which links no counter binary, so the results above are unaffected. The counter arm's own binary_commit records what it actually ran.",
    "campaign.toml declares the seed split for the counter phase only. The AuRUS repeats were split by aurus_campaign.py --repeat-offset instead, which is why the host ranges here read 0-14 and 15-29 rather than the 0-9 and 10-19 that file names."
  ],
  "vendored_scripts": {
    "vendored_on": "2026-08-17",
    "note": "Verbatim copy of the driver that ran the AuRUS arm, so the directory reproduces without the git history. Verify with `git hash-object <file>`. aurus_validate.py is deliberately absent: it has not been run against these results yet, and vendoring the revision that eventually scores them is part of closing the campaign.",
    "files": {
      "aurus_campaign.py": {
        "source_commit": "dea51b7b2f7a7179229c6ebc74c8b4c4f6bc9a55",
        "blob": "c4805050b6984c52c9f4cde71f2e383bd7f2601d",
        "reachable_from_main": false,
        "held_by_tag": "provenance/aurus-h2h-arm",
        "basis": "recorded: staged to both hosts at launch, and both on-disk copies still hash to this blob (checked 2026-08-17). The branch feat/aurus-h2h is not yet merged, so this commit is not an ancestor of main, and the 2026-08-17 rebase took it off the branch as well; the tag holds it. The blob is unchanged by the rebase, so `git hash-object` still verifies this copy."
      }
    }
  }
}
